
VITE_DEV_API_TARGET=https://h38aqomaxk.execute-api.us-east-1.amazonaws.com/
VITE_API_BASE_URL=https://h38aqomaxk.execute-api.us-east-1.amazonaws.com/
VITE_API_TIMEOUT_MS=30000
VITE_APP_TITLE=Safety View - COSMOS
VITE_QS_DATASET_IDENTIFIER=SV_Golden_layer_Table_V3

# Optional: public HTTPS URL to column_dataset_map.json in S3. When set, the
# app fetches it at runtime and falls back to the bundled
# column_dataset_map.json if the request fails or the var is unset.
VITE_COLUMN_MAP_S3_URL=
